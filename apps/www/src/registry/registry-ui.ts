import type { Registry } from "shadcn-vue/schema"

export const ui: Registry["items"] = [
  // UI components will be populated by crawl-content.ts
  // This file serves as the export point for all UI registry items
]
