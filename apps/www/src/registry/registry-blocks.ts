import type { Registry } from "shadcn-vue/schema"

export const blocks: Registry["items"] = [
  // Block components will be populated by crawl-content.ts
  // This file serves as the export point for all block registry items
]
