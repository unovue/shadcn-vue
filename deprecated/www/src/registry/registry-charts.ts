import type { Registry } from "shadcn-vue/schema"

export const charts: Registry["items"] = [
  // Chart components will be populated by crawl-content.ts
  // This file serves as the export point for all chart registry items
]
